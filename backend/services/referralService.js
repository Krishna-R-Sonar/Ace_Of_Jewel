export const createReferral = (user, referralCode) => {
    // logic for creating a referral
    return {user, referralCode};
};

export const getReferrals = (userId) => {
    // logic for retrieving referrals for a user
    return [];
};